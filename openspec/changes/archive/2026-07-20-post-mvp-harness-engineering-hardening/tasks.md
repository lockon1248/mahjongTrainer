<!--
本 tracker 同時是本 change 的唯一 Verification Evidence ledger。
每個 regression 必須先記錄 RED 實際輸出，再進入對應 GREEN task。
-->

## 1. Node 22 runtime gate

- [x] 1.1 以 `HE-NODE-001` 為 **Repository Node runtime gate** 建立 RED：測試 `Runtime behavior and interfaces` 的 `.nvmrc` 解析、Node 18 拒絕、Node 22 接受與正式 package entry 清單；先執行 `nvm use 22 && npx vitest run tests/smoke/node-version-policy.test.ts` 並在 evidence ledger 記錄因 policy 尚不存在而產生的非零 exit code。
- [x] 1.2 依 **Fail fast at every package entry with the repository Node policy** 實作純 policy、CLI、`engines.node = 22.x`、`engine-strict=true` 與全部 npm pre-scripts；以 `nvm exec 18 npm test -- tests/smoke/node-version-policy.test.ts` 證明固定訊息與 exit 1，並以 `nvm use 22 && npm test -- tests/smoke/node-version-policy.test.ts` 證明 preflight 與 regression exit 0。

## 2. Workflow authority normalization

- [x] 2.1 以 `HE-WORKFLOW-001` 為 **Machine-checkable workflow authority** 建立 RED，涵蓋 **Separate audit, apply, verification, and closure as an explicit state machine** 與 `Workflow behavior`：`tests/docs/agents-workflow-policy.test.ts` 必須先因現有重複 heading ID、缺少唯一 state transition 或互斥 closure 條款而以非零 exit code 失敗，並把命令與摘要記入 evidence ledger。
- [x] 2.2 整理 `AGENTS.md` 為唯一 numbered H2、保留非衝突 hard gates，並落實 **Treat post-MVP one-off maintenance as self-contained unless a versioned board is explicitly established** 與 `Scope boundaries`；以 `nvm use 22 && npm test -- tests/docs/agents-workflow-policy.test.ts` exit 0 及 `git diff --check` 證明 audit/repair/verification/closure authority、post-MVP standalone 與 mainline lifecycle 不再矛盾。

## 3. Reachable round harness and fixture migration

- [x] 3.1 以 `HE-ROUND-001` 為 **Reachable round scenario harness** 建立 RED，依 **Build injected rounds through production transitions and invariant checks** 與 `Round scenario behavior and data shape` 測試合法 claim/draw/win 案例，以及 phase/outcome、claim/discard、wall/draw、winning effective count、實體牌數上限等矛盾案例；先執行 `nvm use 22 && npm test -- tests/core/round-scenario-harness.test.ts` 並記錄缺少 harness 的非零 exit code。
- [x] 3.2 實作合法實體牌池、`assertRoundScenarioInvariants` 與四個 production-transition scenario builders，讓 3.1 的合法案例回傳 `BaselineRoundState`、非法案例拋出具名 invariant；以同一 `tests/core/round-scenario-harness.test.ts` 命令 exit 0 與 `nvm use 22 && npm run typecheck` exit 0 驗證。
- [x] 3.3 以 `HE-CLAIM-001` 落實 **Lowest-layer regression first**：先讓 claim-priority regression 明確斷言 win 與 pon 同時為合法候選並觀察舊 fixture RED，再改用 reachable claim-window builder，使 configured priority 選中 win；以 `nvm use 22 && npm test -- tests/core/round-flow-claims.test.ts` exit 0 驗證。
- [x] 3.4 以 `HE-MAINLINE-001` 將 mainline playable claim/draw 與受影響 store/dealer fixtures 改為共享 reachable scenario，不再直接拼接衍生 phase/outcome；以 `nvm use 22 && npm test -- tests/ui/mainline-playable-flow.test.ts tests/ui/game-session.store.test.ts tests/core/dealer-progression.test.ts` exit 0 驗證每個 fixture 通過 invariant checker。
- [x] 3.5 以 `HE-E2E-DRAW-001` 與 `HE-E2E-AI-WIN-001` 將 draw-next-round 與 AI win reveal browser seeds 改為 production draw/win transitions，確保 draw wall 為空且 AI winner 有合法 17 張有效牌；以第 3.1 的 harness regression 與第 5.1 browser journey exit 0 共同驗證，不以直接建立 `RoundResult` 取代 production resolution。

## 4. Single draw dealer oracle

- [x] 4.1 以 `HE-DRAW-001` 為 **Next-round dealer progression after draw** 建立 RED，明確反證已移除的 **Draw outcome stays unresolved for undecided post-draw rules**：測試 `Use same-dealer continuation as the only draw oracle` 與 `Draw behavior`，要求 baseline configured true、draw unresolved 不含 `dealer-continuation`、next round 保留原 dealer；執行 `nvm use 22 && npm test -- tests/core/rule-config-core.test.ts tests/core/round-flow-outcome.test.ts tests/core/dealer-progression.test.ts` 並記錄現況非零 exit code。
- [x] 4.2 將 baseline dealer continuation 設為 configured `true`，讓 production exhaustive draw 只保留 ready-hand unresolved policies，並以 reachable draw fixture 驗證同莊下一局；`nvm use 22 && npm test -- tests/core/rule-config-core.test.ts tests/core/round-flow-outcome.test.ts tests/core/dealer-progression.test.ts tests/ui/game-session.store.test.ts` 必須 exit 0，且不得新增查聽或流局結算。

## 5. Layer verification and evidence closure

- [x] 5.1 依 **Interactive regression closure** 執行 draw result → next round 與 AI win reveal 的真實 browser journeys；`nvm use 22 && npm run test:e2e -- e2e/game-table.smoke.spec.ts` 必須 exit 0，並在 evidence ledger 記錄涵蓋的跨 phase 流程；若環境阻擋，必須記錄精確 command、path/service 與 exit result，且不得宣告 E2E closure。
- [x] 5.2 依 `Acceptance criteria` 執行 Node 22 下的完整 `npm test`、`npm run typecheck`、`npm run build`、受影響 browser E2E、`git diff --check` 與 `spectra validate post-mvp-harness-engineering-hardening`；所有實際 exit code 必須為 0 才可勾選，本 task 不授權 archive 或 mainline sync。
- [x] 5.3 依 **Verification evidence ledger** 與 **Make verification evidence part of change truth** 將下表每個 regression 的 reproduction、最低錯誤層、RED/GREEN/E2E 精確命令、實際 exit code 與摘要補齊；以人工內容審查確認不得出現空白證據或用 `all_done`、typecheck/build 代替行為證據。

## Verification Evidence ledger

| Regression ID | Reproduction | Lowest wrong layer | RED command / exit / summary | GREEN command / exit / summary | Browser E2E command / exit / journey |
| --- | --- | --- | --- | --- | --- |
| HE-NODE-001 | 非 Node 22 執行正式 package entry | Tooling/runtime | `source "$NVM_DIR/nvm.sh" && nvm use 22 >/dev/null && npx vitest run tests/smoke/node-version-policy.test.ts` / exit 1 / test suite 因 `scripts/node-version-policy.mjs` 不存在而無法載入，證明 repo 沒有可執行 Node policy | `source "$NVM_DIR/nvm.sh" && nvm exec 18 npm test -- tests/smoke/node-version-policy.test.ts` / exit 1 / pretest 在 Vitest 前拒絕 v18.20.8 並顯示固定 `nvm use` 訊息；`source "$NVM_DIR/nvm.sh" && nvm use 22 >/dev/null && npm test -- tests/smoke/node-version-policy.test.ts` / exit 0 / 1 file、3 tests passed | N/A |
| HE-WORKFLOW-001 | 重複或互斥的 hard-gate 結構仍能存在 | Workflow policy | `source "$NVM_DIR/nvm.sh" && nvm use 22 >/dev/null && npm test -- tests/docs/agents-workflow-policy.test.ts` / exit 1 / 3 tests failed：26 個 numbered headings 僅 23 個唯一 ID，缺少 Post-MVP 與 Repair/Closure authority 標題，且沒有唯一 state transition | `source "$NVM_DIR/nvm.sh" && nvm use 22 >/dev/null && npm test -- tests/docs/agents-workflow-policy.test.ts` / exit 0 / 1 file、3 tests passed；`git diff --check` exit 0 | N/A |
| HE-ROUND-001 | 半有效 `BaselineRoundState` 未被拒絕 | Core test harness | `source "$NVM_DIR/nvm.sh" && nvm use 22 >/dev/null && npm test -- tests/core/round-scenario-harness.test.ts` / exit 1 / test suite 因 `src/core/testing/roundScenario.ts` 不存在而無法載入 | `source "$NVM_DIR/nvm.sh" && nvm use 22 >/dev/null && npm test -- tests/core/round-scenario-harness.test.ts` / exit 0 / 1 file、7 tests passed；合法 claim/draw/discard-win/self-draw 皆由 production transition 建立，五類矛盾 fixture 皆被具名 invariant 拒絕；`npm run typecheck` exit 0 | N/A |
| HE-CLAIM-001 | 不合法 pon fixture 被當成 competing claim 證據 | Core/rules fixture | `source "$NVM_DIR/nvm.sh" && nvm use 22 >/dev/null && npm test -- tests/core/round-flow-claims.test.ts` / exit 1 / claim-priority test 顯示 west 合法候選只有 `pass`，沒有測到 competing pon | 同一命令 / exit 0 / 1 file、9 tests passed；reachable production discard fixture 同時產生 east win 與 west pon，configured priority 選中 win | N/A |
| HE-MAINLINE-001 | mainline/store draw 或 claim 直接拼接衍生 state | Store/integration fixture | `source "$NVM_DIR/nvm.sh" && nvm use 22 >/dev/null && npm test -- tests/core/round-scenario-harness.test.ts` / exit 1 / shared harness 模組不存在，既有 mainline/store fixture 只能直接拼接 phase、pending window 與 draw outcome | `source "$NVM_DIR/nvm.sh" && nvm use 22 >/dev/null && npm test -- tests/ui/mainline-playable-flow.test.ts tests/ui/game-session.store.test.ts tests/core/dealer-progression.test.ts` / exit 0 / 3 files、33 tests passed；claim/draw fixture 由共享 production-transition builder 建立並通過 invariant checker | N/A；此 regression 僅涵蓋 core/store fixture，跨 phase 旅程分別由 HE-E2E-DRAW-001 與 HE-E2E-AI-WIN-001 關閉 |
| HE-DRAW-001 | 65 張起始牌牆經 east discard 與全員 pass 進入 production exhaustive draw | Core/config + round flow | `source "$NVM_DIR/nvm.sh" && nvm use 22 && npm test -- tests/core/rule-config-core.test.ts tests/core/round-flow-outcome.test.ts tests/core/dealer-progression.test.ts` / exit 1 / 5 tests failed：baseline config 與 draw metadata 仍含 unresolved dealer continuation | `source "$NVM_DIR/nvm.sh" && nvm use 22 && npm test -- tests/core/rule-config-core.test.ts tests/core/round-flow-outcome.test.ts tests/core/dealer-progression.test.ts tests/ui/game-session.store.test.ts` / exit 0 / 4 files、32 tests passed；同莊下一局且 metadata 僅保留 ready-hand unresolved | Covered by HE-E2E-DRAW-001 |
| HE-E2E-DRAW-001 | 牌牆耗盡後經 discard → 全員 pass → exhaustive draw，按下一局必須由原東家續莊 | E2E seed + round flow | `source "$NVM_DIR/nvm.sh" && nvm use 22 && npm test -- tests/core/rule-config-core.test.ts tests/core/round-flow-outcome.test.ts tests/core/dealer-progression.test.ts` / exit 1 / reachable draw regression 顯示 outcome 多出 `dealer-continuation` | `source "$NVM_DIR/nvm.sh" && nvm use 22 && npm test` / exit 0 / 37 files、160 tests passed | `source "$NVM_DIR/nvm.sh" && nvm use 22 >/dev/null && npm run test:e2e -- e2e/game-table.smoke.spec.ts` / exit 0 / 17 Chromium journeys passed；流局結果 → 籌碼結算 → 下一局 → 東家仍為莊家、17 張起手、無錯誤 |
| HE-E2E-AI-WIN-001 | AI 手牌被直接標記為 self-draw win，沒有經合法 17 張自摸 action | E2E seed + scoring/action | `source "$NVM_DIR/nvm.sh" && nvm use 22 >/dev/null && npm test -- tests/core/round-scenario-harness.test.ts` / exit 1 / source contract 顯示 AI win seed 直接呼叫 `createWinRoundResult`，未經 reachable builder 或 production action | 同一命令 / exit 0 / 1 file、8 tests passed；AI seed 由合法 17 張 self-draw opportunity 建立並透過 `store.advanceTurn()` 執行 production win transition | `source "$NVM_DIR/nvm.sh" && nvm use 22 >/dev/null && npm run test:e2e -- e2e/game-table.smoke.spec.ts` / exit 0 / 17 Chromium journeys passed；AI 自摸後南家得勝牌區揭露合法 17 張牌 |
